const numChildren = 5;
const partnerName = "Victoria";
const geoLocation = "California";
const jobTitle = "Broker";

console.log(
  `You will be a ${jobTitle} in ${geoLocation}, and married to ${partnerName} with ${numChildren} kids.`
);

const birthYear = 1990;
const futureYear = 2050;
const age1 = futureYear - birthYear;
const age2 = age1 - 1;

console.log(`I will be either ${age1} or ${age2} in ${futureYear}.`);
